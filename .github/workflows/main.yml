on: [push]

jobs:
  testCIAction:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Hello world action step
        uses: stopa323/h8s-ci
        id: hello
        with:
          slackBotToken: ${{ secrets.SLACK_BOT_TOKEN }}
          slackChannel: C01667Y1K8U
      - name: Some random step
        run: echo "123"
